# CleanBot v2.0 依赖
# 国内用户: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# GUI 框架
PyQt6>=6.5.0

# 系统监控
psutil>=5.9.0

# 安全删除 (移到回收站)
send2trash>=1.8.0

# 图像处理 (机器人形象)
Pillow>=10.0.0

# Windows API (可选 — 安装失败不影响核心功能)
# pywin32>=306
