# CleanBot v2.0 依赖
# 国内用户: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# GUI 框架
PyQt6>=6.5.0

# 系统监控
psutil>=5.9.0
pywin32>=306

# 安全删除 (移到回收站)
send2trash>=1.8.0

# 图像处理 (机器人形象)
Pillow>=10.0.0

# 打包工具 (仅开发时需要)
# pyinstaller>=6.0.0
